
namespace NoMansBlocks.Logging {
    /// <summary>
    /// The various types of log messages that can be
    /// printed out to the console / log file.
    /// </summary>
    public enum LogStatementType {
        Debug = 0,
        Warning = 1,
        Error = 2,
    }
}