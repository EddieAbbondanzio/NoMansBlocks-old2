namespace NoMansBlocks.Serialization {
    /// <summary>
    /// What kind of compression to use for storing / reading 
    /// the file from disk.
    /// </summary>
    public enum CompressionType {
        None = 0,
        GZip = 1,
    }
}
